<ul class="list-unstyled">
  <% posts.each do |post| %>
    <li class="media mb-3">
      <div class="media-body">
        <div>
          
          <%= link_to post.user.name, post_path(post.id) %> ←詳細<span class="text-muted">  posted at <%= post.created_at %></span>
      
        </div>
        <div class="border-bottom">
          <p><strong>area:</strong><%= post.area %></p>
          <p><strong>country:</strong><%= post.country %></p>
          <p><strong>place:</strong><%= post.place %></p>
          <p class="text-justify"><strong>comment:</strong><%= post.comment %></p>
          <p>
          <% if post.photo? %>
          <strong>photo:</strong>
          <%# app/assets/images %>
          <%= image_tag post.photo.url, alt: "Rails photo"%>
          <% end %>
          </p>
        </div>
      </div>
    </li>
  <% end %>
  <%= paginate posts %>
</ul>