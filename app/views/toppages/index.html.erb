
<% if logged_in? %>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
   <h1>次の旅先を見つけよう</h1>
     <div class="border-bottom">
   <h2>投稿一覧</h2>
     </div>
   <%= render 'microposts/micropost', posts: @posts %>
<% else %>
<h1>次の旅先を見つけよう</h1>
   <div class="border-bottom">
   <h2>投稿一覧</h2>
   </div>
   <%= render 'microposts/micropost', posts: @posts %>
<% end %>